service:
  db:
    image: leogloriainfnet/postgres-tp2-spring
    command: sh -c "npm install && npm run dev"
    ports:
      - "5432:5432"
    container_name: TP2_SpringBoot
    restart: always
    enviroment:
        POSTGRES_PASSWORD=1234
        POSTGRES_USER=Aluno


